diff -ur Makefile Makefile
--- Makefile	2006-04-09 16:38:50.000000000 -0400
+++ Makefile	2008-02-17 16:43:31.640625000 -0500
@@ -107,7 +107,7 @@
 all: iDoom
 
 iDoom: $(OBJS) Makefile
-	$(CC) $(OBJS) -o iDoom $(CFLAGS) $(LDFLAGS)
+	$(CC) $(OBJS) -o iFreeDoom $(CFLAGS) $(LDFLAGS)
 
 clean: 
-	$(RM) $(OBJS) *~ iDoom iDoom.gdb
+	$(RM) $(OBJS) *~ iFreeDoom iFreeDoom.gdb
diff -ur d_main.c d_main.c
--- d_main.c	2008-02-17 16:37:12.656250000 -0500
+++ d_main.c	2008-02-17 16:42:51.500000000 -0500
@@ -591,9 +591,9 @@
     if (!doomwaddir)
 	doomwaddir = "/opt/iDoom/IWADs";
 
-    // Commercial.
-    doom2wad = malloc(strlen(doomwaddir)+1+9+1);
-    sprintf(doom2wad, "%s/doom2.wad", doomwaddir);
+    // FreeDoom
+    doom2wad = malloc(strlen(doomwaddir)+1+12+1);
+    sprintf(doom2wad, "%s/freedm.wad", doomwaddir);
 
     // Retail.
     doomuwad = malloc(strlen(doomwaddir)+1+8+1);
