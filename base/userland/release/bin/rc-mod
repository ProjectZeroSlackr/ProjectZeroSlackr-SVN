#!/bin/sh

###########################################
# ZeroSlackr's Modified iPL Boot Script
# Modified rc script by Keripo
# For DataGhost's loop-mount boot method
# Last updated: August 28, 2007
###########################################

# Note: File best edited with an advanced
# text editor such as Notepad++ or equivalent.
# Do NOT use Notepad/Wordpad; Podwrite is good.
# To disable a line, place a "#" infront of it.

# Nice little note
/bin/busybox echo " "
/bin/busybox echo "======================"
/bin/busybox echo " Keripo's ZeroSlackr  "
/bin/busybox echo "  Loop-Mount Method   "
/bin/busybox echo "======================"
/bin/busybox echo " "

# Mount modified ZeroSlackr userland
/bin/busybox mount -t ext3 -o loop,rw /bin/ZeroSlackr-Userland.ext3 /bin/mount

# Swap looped partition
cd /bin/mount/
/bin/busybox pivot_root . mnt

# Execute normal rc file
exec /bin/sh /etc/rc
